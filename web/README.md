# Talia Travels frontend

React and TypeScript frontend powered by Vite and Sanity.

## Local setup

1. Copy `.env.example` to `.env.local`.
2. Run `npm install`.
3. Run `npm run dev`.

The application reads published public content from Sanity without an API token.
